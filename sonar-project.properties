sonar.projectKey=CustomizableBlackjack
sonar.organization=kobystestorg

sonar.python.coverage.reportPaths=coverage.xml

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=Blackjack
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=src/

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

# Coverage report path
sonar.python.coverage.reportPaths=coverage.xml

# Optional: Specify the programming language
sonar.language=python

# Optional: Exclude files or directories from the analysis
# sonar.exclusions=**/__pycache__/**, **/*.test.py
